pipeline {

    agent any

    tools {
        maven 'M2_HOME'
    }

    stages {
        stage ("sonarqube scan") {
            steps {
                withSonarQubeEnv ('sonarserver') {
                sh 'mvn verify org.sonarsource.scanner.maven:sonar-maven-plugin:sonar -Dsonar.projectKey=laridevops_week16-geoapp'
                }
            }
        }

        stage ("all maven commands") {
            steps {
                sh 'mvn clean test compile install package'
            }
        }

        stage ("uplaod artifact") {
            steps {
                sh 'curl -uadmin:AP93kMYoP1b5AfisPKN4EZn94g4 -T target/bioMedical-0.0.2-SNAPSHOT.jar "http://ec2-44-205-243-105.compute-1.amazonaws.com:8081/artifactory/geoapp/'
            }
        }

        }
    }