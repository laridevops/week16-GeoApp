pipeline {
    agent any
    stages{
        stage('check current directory'){
            agent {docker { image 'node:20.10.0-alpine3.19' }}
            steps{
                sh 'pwd'
               sh 'id'
               sh "mkdir \$(date '+%Y%m%d_%H%M%S')"
                sh 'npm --version'
            }
        }
    }
}